import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LoanCycleParameterPrimaryIndViewComponent } from './loan-cycle-parameter-primary-ind-view.component';

describe('LoanCycleParameterPrimaryIndViewComponent', () => {
  let component: LoanCycleParameterPrimaryIndViewComponent;
  let fixture: ComponentFixture<LoanCycleParameterPrimaryIndViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LoanCycleParameterPrimaryIndViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LoanCycleParameterPrimaryIndViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
