import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SubDistrictMasterIndViewComponent } from './sub-district-master-ind-view.component';

describe('SubDistrictMasterIndViewComponent', () => {
  let component: SubDistrictMasterIndViewComponent;
  let fixture: ComponentFixture<SubDistrictMasterIndViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SubDistrictMasterIndViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SubDistrictMasterIndViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
