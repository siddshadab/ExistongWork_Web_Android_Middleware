import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LoanApprovalLimitComponent } from './loan-approval-limit.component';

describe('LoanApprovalLimitComponent', () => {
  let component: LoanApprovalLimitComponent;
  let fixture: ComponentFixture<LoanApprovalLimitComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LoanApprovalLimitComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LoanApprovalLimitComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
