import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LookupMasterViewComponent } from './lookup-master-view.component';

describe('LookupMasterViewComponent', () => {
  let component: LookupMasterViewComponent;
  let fixture: ComponentFixture<LookupMasterViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LookupMasterViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LookupMasterViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
