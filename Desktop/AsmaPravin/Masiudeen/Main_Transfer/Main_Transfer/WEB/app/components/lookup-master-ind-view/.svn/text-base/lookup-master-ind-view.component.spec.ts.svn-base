import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LookupMasterIndViewComponent } from './lookup-master-ind-view.component';

describe('LookupMasterIndViewComponent', () => {
  let component: LookupMasterIndViewComponent;
  let fixture: ComponentFixture<LookupMasterIndViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LookupMasterIndViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LookupMasterIndViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
