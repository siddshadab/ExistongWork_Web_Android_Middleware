import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SubDistrictMasterViewComponent } from './sub-district-master-view.component';

describe('SubDistrictMasterViewComponent', () => {
  let component: SubDistrictMasterViewComponent;
  let fixture: ComponentFixture<SubDistrictMasterViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SubDistrictMasterViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SubDistrictMasterViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
