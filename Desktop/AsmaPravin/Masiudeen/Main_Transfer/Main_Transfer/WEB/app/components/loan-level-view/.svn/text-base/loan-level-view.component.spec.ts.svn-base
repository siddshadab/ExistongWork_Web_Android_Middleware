import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LoanLevelViewComponent } from './loan-level-view.component';

describe('LoanLevelViewComponent', () => {
  let component: LoanLevelViewComponent;
  let fixture: ComponentFixture<LoanLevelViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LoanLevelViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LoanLevelViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
