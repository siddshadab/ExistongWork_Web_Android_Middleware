import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CountryMasterViewComponent } from './country-master-view.component';

describe('CountryMasterViewComponent', () => {
  let component: CountryMasterViewComponent;
  let fixture: ComponentFixture<CountryMasterViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CountryMasterViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CountryMasterViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
