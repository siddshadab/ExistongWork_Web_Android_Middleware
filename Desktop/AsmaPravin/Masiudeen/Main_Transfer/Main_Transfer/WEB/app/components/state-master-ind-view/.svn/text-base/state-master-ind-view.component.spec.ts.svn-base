import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StateMasterIndViewComponent } from './state-master-ind-view.component';

describe('StateMasterIndViewComponent', () => {
  let component: StateMasterIndViewComponent;
  let fixture: ComponentFixture<StateMasterIndViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StateMasterIndViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StateMasterIndViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
