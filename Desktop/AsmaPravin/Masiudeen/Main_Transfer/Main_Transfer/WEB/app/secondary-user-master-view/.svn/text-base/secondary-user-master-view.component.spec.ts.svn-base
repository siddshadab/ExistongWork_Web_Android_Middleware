import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SecondaryUserMasterViewComponent } from './secondary-user-master-view.component';

describe('SecondaryUserMasterViewComponent', () => {
  let component: SecondaryUserMasterViewComponent;
  let fixture: ComponentFixture<SecondaryUserMasterViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SecondaryUserMasterViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SecondaryUserMasterViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
