import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SecondaryUserMasterIndViewComponent } from './secondary-user-master-ind-view.component';

describe('SecondaryUserMasterIndViewComponent', () => {
  let component: SecondaryUserMasterIndViewComponent;
  let fixture: ComponentFixture<SecondaryUserMasterIndViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SecondaryUserMasterIndViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SecondaryUserMasterIndViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
